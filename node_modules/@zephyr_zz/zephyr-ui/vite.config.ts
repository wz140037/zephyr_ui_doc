import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  build: {
    lib: {
      entry: './packages/index.ts',
      name: 'zephyrUi',
      fileName: (format) => `zephyrUi.${format}.ts`,
      formats: ['es', 'umd']
    },
    rollupOptions: {
      external: ['vue'],
      output: {
        globals: {
          vue: 'Vue'// 抛出一个全局对象
        }
      }
    }
  }
})